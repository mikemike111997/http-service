apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: http-server
    language: python
  name: http-server
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: http-server
      language: python
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: http-server
        language: python
    spec:
      containers:
      - image: gcr.io/fourth-ability-322319/github.com/mikemike111997/http-service@sha256:1375720a8334d2ab90d8b6bc6f90599454d42e8bdc2bfd89805048ecb2db88fc
        imagePullPolicy: IfNotPresent
        name: http-service
      restartPolicy: Always

